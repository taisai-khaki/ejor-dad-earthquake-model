# Superseded computation package

This package is retained for audit history only and must not be used for manuscript claims. The continuous B&B and synthetic scalability outputs accepted over-budget upper-corner evaluations as incumbents, and the robustness-equity frontier fixed recourse decisions instead of reoptimizing them. Use `critical_revision_v2` after its corrected rerun completes.